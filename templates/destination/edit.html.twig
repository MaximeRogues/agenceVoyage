{% extends 'base.html.twig' %}

{% block body %}

<h2>Modifier une destination</h2>
<br>

{{ form(form) }}

{% if errors %}
    <ul>
        {% for error in errors %}
            <li>
                {{error.message}}
            </li>
        {% endfor %}
    </ul>
{% endif %}

{% endblock %}
